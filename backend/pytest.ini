[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: requires a real PostgreSQL and Redis instance
    slow: marks tests as slow running
addopts = -v --tb=short --no-header -q
